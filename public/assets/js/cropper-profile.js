document.addEventListener('DOMContentLoaded',()=>{const input=document.getElementById('fotoPerfilInput');if(!input)return;const img=document.getElementById('cropperImage');const preview=document.getElementById('fotoPerfilPreview');const modalEl=document.getElementById('cropperModal');let cropper;const modal=new bootstrap.Modal(modalEl);input.addEventListener('change',e=>{const f=e.target.files[0];if(!f)return;img.src=URL.createObjectURL(f);modal.show();setTimeout(()=>{cropper?.destroy();cropper=new Cropper(img,{aspectRatio:1,viewMode:1});},200);});document.getElementById('salvarCrop')?.addEventListener('click',()=>{if(!cropper)return;preview.src=cropper.getCroppedCanvas({width:300,height:300}).toDataURL();modal.hide();});});